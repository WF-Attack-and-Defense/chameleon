[default]
; Directory containing Surakav training artifacts:
;   - scaler.gz   (joblib MinMaxScaler)
;   - generator_seqlen<LEN>_cls<N>_latentdim<D>.ckpt
; Train with pendding/surakav (mlp_df_wgan_train.py, etc.) then point here.
model_dir = ../checkpoints/surakav_wfgan
; Dummy cell marker (original Surakav uses 888)
dummy_code = 888
seed = 42
