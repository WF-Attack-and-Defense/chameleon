[default]
# Directory containing df_weights.pth (optional) and generator_site_<k>.pt per monitored class.
model_dir = ../checkpoints/
# Burst tensor length (must match training).
max_length = 5000
# Overhead band used during training (inference uses saved generators only).
oh_min_threshold = 0.10
oh_max_threshold = 0.50
